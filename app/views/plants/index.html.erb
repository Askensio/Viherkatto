<% provide(:title, 'Kasvit') %>

<% flash.each do |key, value| %>
    <div class="alert alert-<%= key %>"><%= value %></div>
<% end %>

<%= will_paginate %>
<ul class="plants">
  <%= render @plants %>
</ul>
<%= will_paginate %>